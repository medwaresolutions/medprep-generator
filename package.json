{
  "name": "medical-pdf-converter",
  "version": "1.0.0",
  "description": "Convert medical PDF instructions to CSV format",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "DEBUG=app:* node test.js",
    "clean": "rm -rf output/* && rm -rf node_modules && rm -f package-lock.json",
    "setup": "mkdir -p uploads output && npm install"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "csv-writer": "^1.6.0",
    "debug": "^4.3.4",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "natural": "^6.10.4",
    "pdf2json": "^2.0.2"
  }
}
